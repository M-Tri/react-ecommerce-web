# SuperSimpleMarket Frontend

React + TypeScript + Vite frontend for the SuperSimpleMarket ecommerce demo.

```bash
npm install
npm run dev
npm run build
npm run lint
npm test
```

The dev server proxies `/api` requests to the backend on `http://localhost:3000`.
